# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Saved browser auth state — contains session cookies, never commit
.auth/

# Environment variables — contains secrets, never commit
.env
